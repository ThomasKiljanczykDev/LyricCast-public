<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!--
      ~ Files handed between this app and another one as content:// URIs. The pending-import copy
      ~ is deliberately not here: nothing outside the app needs to read it.
      -->
    <cache-path
        name="shared"
        path="shared/" />
</paths>
